<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel="icon" href="/public/favicon.ico" sizes="70x70" type="image/ico">

    <link rel="shortcut icon" href="#">
    <link href="/public/css/my_style.css" type="text/css" rel="stylesheet">

    {{#each js}}
      <script defer src="/public/js/{{this}}"></script>
    {{/each}}

  </head>
  <body>
    {{!-- fix later, can't get the error message to pop up. --}}
    {{!-- {{#if (nonEmptyObject messages)}}
      {{>flash-message}}
    {{/if}} --}}
    {{!-- {{log(nonEmptyObject messages)}} --}}
    {{>header}}
    {{>nav}}
    {{{body}}}
    {{>footer}}
  </body>
  {{#if isLoggedIn}}
    <script>
      document.getElementById('logout-button')
      .addEventListener('click', function(ev){
        fetch('/users/logout', {method:"POST"})
          .then(response => response.json())
          .then(function(res_json){
            console.log(res_json);
            window.location.replace('/');
          })
          .catch(err => console.log(err));
      })
  {{/if}}
    </script>
</html>
